#ifndef _ROTATED_ARRAY_H_
#define _ROTATED_ARRAY_H_

/*
 * Determines if b is a rotated version of a
 * e.g. a = {1,2,3,5,6,7,8}, rotated b = {5,6,7,8,1,2,3}, {6,7,8,1,2,3,5}, ...
 *
 * Returns 1 if b is rotated, 0 if not
 */
int rotated_array(int a[], int b[], int length);

#endif
